# Sample vrscore config — paths are relative to this file's directory.
topology = "topology.json"
findings = "findings.json"
format = "table"
summary = true
explain = false
min_priority = 0.0
sort = "priority"
