# CI gate defaults — pair with --fail-under on the job or set here.
topology = "topology.json"
findings = "findings.json"
format = "sarif"
output = "reachability.sarif"
summary = true
quiet = true
only_reachable = true
max_hops = 3
fail_under = 7.0
dedupe = true
sort = "priority"
