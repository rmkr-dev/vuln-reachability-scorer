# CI gate defaults — extends shared paths.
extends = "vrscore-base.toml"
format = "sarif"
output = "reachability.sarif"
summary = true
quiet = true
only_reachable = true
max_hops = 3
fail_under = 7.0
dedupe = true
sort = "priority"
